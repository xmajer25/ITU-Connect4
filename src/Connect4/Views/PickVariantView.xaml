<Page x:Class="Connect4.Views.PickVariantView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewModel="clr-namespace:Connect4.ViewModel"
      xmlns:local="clr-namespace:Connect4.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PickVariantView">
    
    <Page.DataContext>
        <viewModel:PickVariantViewModel/>
    </Page.DataContext>
    
    <Grid>
        <StackPanel Orientation="Vertical"
                    HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Standard" Margin="10"
                    Width="150" Height="40"/>
            <Button Content="Crazy House" Margin="10"
                    Width="150" Height="40"/>
            <Button Content="Back" Margin="10"
                    Command="{Binding NavigateToMenuCommand}"
                    Width="150" Height="40"/>
        </StackPanel>
    </Grid>
</Page>
