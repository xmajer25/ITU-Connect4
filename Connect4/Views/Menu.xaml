<Window x:Class="Connect4.Views.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:Connect4.ViewModel"
        xmlns:local="clr-namespace:Connect4.Views"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Window.DataContext>
        <viewModel:MenuViewModel/>
    </Window.DataContext>

    <Grid>
        <Button Content="Go to Settings"
                Command="{Binding NavigateToSettingsCommand}"
                Width="150" Height="40" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <TextBlock Name="ResultText" Text="Výsledek zde..." VerticalAlignment="Top" HorizontalAlignment="Center"/>
    </Grid>
</Window>
